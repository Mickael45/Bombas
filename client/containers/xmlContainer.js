import { connect } from 'react-redux'
import Xml from './../components/xml'

const mapDispatchToProps = (dispatch) => ({
})

const mapStatesToProps = (state) => {
  return {
  }
}

module.exports = connect(mapStatesToProps, mapDispatchToProps)(Xml)
